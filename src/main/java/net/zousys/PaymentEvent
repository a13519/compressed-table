package com.example.clearmatch.model;

import java.math.BigDecimal;

public record PaymentEvent(
        String endToEndId,
        String txId,
        BigDecimal amount,
        String currency,
        String debtorName
) {}
